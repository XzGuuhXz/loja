import { notFound } from "next/navigation";
import Link from "next/link";
import { prisma } from "@/lib/prisma";
import { AddToCart } from "@/components/cart/add-to-cart";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await prisma.product.findUnique({where:{slug},select:{name:true,description:true}});return p?{title:p.name,description:p.description}:{title:"Produto não encontrado"}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await prisma.product.findUnique({where:{slug},include:{category:true,images:{orderBy:{position:"asc"}}}});if(!p||!p.isActive)notFound();const price=Number(p.price);return <main className="min-h-screen bg-slate-50 px-6 py-10 text-slate-950"><div className="mx-auto max-w-6xl"><Link href="/produtos" className="text-sm font-bold text-indigo-600">← Voltar para produtos</Link><div className="mt-8 grid gap-10 lg:grid-cols-2"><div className="grid aspect-square place-items-center rounded-3xl bg-gradient-to-br from-indigo-100 via-white to-violet-100 text-7xl font-black text-indigo-200">NV</div><section className="py-4"><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">{p.category.name}</p><h1 className="mt-3 font-[family-name:var(--font-jakarta)] text-4xl font-extrabold">{p.name}</h1><p className="mt-6 text-3xl font-black">{new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(price)}</p><p className="mt-6 leading-7 text-slate-600">{p.description}</p><div className="mt-8 rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">Estoque disponível</p><p className="mt-1 font-bold">{p.stock} unidade(s)</p><div className="mt-5"><AddToCart product={{id:p.id,slug:p.slug,name:p.name,price,stock:p.stock,imageUrl:p.images[0]?.url}}/></div></div><p className="mt-4 text-xs text-slate-400">SKU: {p.sku}</p></section></div></div></main>}
