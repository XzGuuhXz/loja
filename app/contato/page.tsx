'use client'; import {useState} from 'react'; import Link from 'next/link';
export default function Contact(){const [sent,setSent]=useState(false);return <main className="page-shell"><Link href="/" className="back-link">← Loja</Link><div className="page-title"><p className="eyebrow">ATENDIMENTO</p><h1>Fale com a gente.</h1></div>{sent?<div className="empty-state"><h2>Mensagem recebida.</h2><p>Obrigado pelo contato. Retornaremos em breve.</p><Link className="loja-btn loja-btn-primary" href="/">Voltar para a loja</Link></div>:<form className="panel form-grid" style={{maxWidth:650}} onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Nome<input required maxLength={120}/></label><label>E-mail<input required type="email" maxLength={160}/></label><label>Mensagem<textarea required rows={6} maxLength={2000} style={{padding:12,border:'1px solid var(--line)',resize:'vertical'}}/></label><button className="primary-btn" type="submit">Enviar mensagem →</button></form>}</main>}