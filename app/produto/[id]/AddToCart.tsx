'use client';
import {useState} from 'react';
export default function AddToCart({product}:{product:{id:string;name:string;price:number}}){const [qty,setQty]=useState(1);const [msg,setMsg]=useState('');function add(){let cart:any[]=[];try{cart=JSON.parse(localStorage.getItem('loja-cart')||'[]')}catch{};const i=cart.findIndex(x=>x.product_id===product.id);if(i>=0)cart[i].quantity=Math.min(100,cart[i].quantity+qty);else cart.push({product_id:product.id,name:product.name,price:product.price,quantity:qty});localStorage.setItem('loja-cart',JSON.stringify(cart));setMsg('Adicionado ao carrinho.')}return <div><input type="number" min={1} max={100} value={qty} onChange={e=>setQty(Math.max(1,Math.min(100,Number(e.target.value)||1)))} /><button onClick={add}>Adicionar ao carrinho</button>{msg&&<span> {msg}</span>}</div>}