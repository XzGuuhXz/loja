import Link from 'next/link';import { notFound,redirect } from 'next/navigation';import { auth } from '@/auth';import { prisma } from '@/lib/prisma';import { OrderControls } from '@/components/admin/order-controls';
const money=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v);
export default async function AdminOrderDetail({params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user||!["ADMIN","SUPER_ADMIN"].includes(s.user.role))redirect('/admin/login');const {id}=await params;const o=await prisma.order.findUnique({where:{id},include:{user:true,address:true,items:true}});if(!o)notFound();return <main className="min-h-screen bg-slate-50 px-6 py-10 text-slate-950"><div className="mx-auto max-w-5xl"><Link href="/admin/pedidos" className="text-sm font-bold text-indigo-600">← Pedidos</Link><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_320px]"><section className="rounded-3xl border border-slate-200 bg-white p-6"><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">Pedido #{o.id.slice(-8).toUpperCase()}</p><h1 className="mt-2 text-3xl font-extrabold">Detalhes do pedido</h1><div className="mt-6 divide-y">{o.items.map(i=><div key={i.id} className="flex items-center justify-between py-4"><div><p className="font-bold">{i.productName}</p><p className="text-sm text-slate-500">{i.quantity} × {money(Number(i.price))}</p></div><strong>{money(Number(i.subtotal))}</strong></div>)}</div><div className="mt-6 space-y-2 border-t pt-5 text-sm"><p className="flex justify-between"><span>Subtotal</span><strong>{money(Number(o.subtotal))}</strong></p><p className="flex justify-between"><span>Frete</span><strong>{money(Number(o.shipping))}</strong></p><p className="flex justify-between text-lg"><span>Total</span><strong>{money(Number(o.total))}</strong></p></div></section><aside className="space-y-6"><div className="rounded-3xl border border-slate-200 bg-white p-6"><h2 className="font-extrabold">Atualizar pedido</h2><div className="mt-4"><OrderControls id={o.id} status={o.status} paymentStatus={o.paymentStatus}/></div></div><div className="rounded-3xl border border-slate-200 bg-white p-6"><h2 className="font-extrabold">Cliente</h2><p className="mt-3 text-sm">{o.user.name}</p><p className="text-sm text-slate-500">{o.user.email}</p><h2 className="mt-5 font-extrabold">Entrega</h2><p className="mt-3 text-sm leading-6">{o.address.recipient}<br/>{o.address.street}, {o.address.number}{o.address.complement?` · ${o.address.complement}`:''}<br/>{o.address.neighborhood} · {o.address.city}/{o.address.state}<br/>{o.address.zipCode}</p></div></aside></div></div></main>}
