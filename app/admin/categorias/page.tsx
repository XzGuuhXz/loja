import {auth} from '@/auth';import {redirect} from 'next/navigation';import {prisma} from '@/lib/prisma';import Link from 'next/link';import {deleteCategory} from '@/actions/catalog';import {AdminActionButton} from '@/components/admin/action-button';
export default async function AdminCategoriesPage(){const s=await auth();if(!s?.user||!["ADMIN","SUPER_ADMIN"].includes(s.user.role))redirect('/admin/login');const categories=await prisma.category.findMany({include:{_count:{select:{products:true}}},orderBy:{name:'asc'}});return <main className="min-h-screen bg-slate-50 px-6 py-10 text-slate-950"><div className="mx-auto max-w-5xl"><div className="flex items-center justify-between"><div><Link href="/admin/dashboard" className="text-sm font-bold text-indigo-600">← Dashboard</Link><h1 className="mt-5 text-3xl font-extrabold">Categorias</h1></div><Link href="/admin/categorias/nova" className="rounded-xl bg-indigo-600 px-5 py-3 text-sm font-bold text-white">Nova categoria</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map(c=><article key={c.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{c.name}</h2><p className="mt-1 text-sm text-slate-500">/{c.slug}</p></div><span className={`rounded-full px-2 py-1 text-xs font-bold ${c.isActive?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{c.isActive?'Ativa':'Inativa'}</span></div><p className="mt-4 text-sm font-semibold">{c._count.products} produto(s)</p><div className="mt-5 flex gap-2"><Link href={`/admin/categorias/${c.id}/editar`} className="rounded-lg bg-slate-950 px-3 py-2 text-xs font-bold text-white">Editar</Link><AdminActionButton label="Excluir" action={()=>deleteCategory(c.id)} confirm="Excluir esta categoria? A exclusão só será permitida se ela não possuir produtos." className="rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-700"/></div></article>)}</div></div></main>}
