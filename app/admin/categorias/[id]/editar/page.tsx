import Link from 'next/link';import {notFound,redirect} from 'next/navigation';import {auth} from '@/auth';import {prisma} from '@/lib/prisma';import {CategoryForm} from '@/components/admin/category-form';export default async function EditCategoryPage({params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user||!["ADMIN","SUPER_ADMIN"].includes(s.user.role))redirect('/admin/login');const {id}=await params;const c=await prisma.category.findUnique({where:{id}});if(!c)notFound();return <main className="min-h-screen bg-slate-50 px-6 py-10"><div className="mx-auto max-w-2xl"><Link href="/admin/categorias" className="text-sm font-bold text-indigo-600">← Categorias</Link><div className="mt-6 rounded-3xl border border-slate-200 bg-white p-8"><h1 className="text-3xl font-extrabold">Editar categoria</h1><CategoryForm category={c}/></div></div></main>}
